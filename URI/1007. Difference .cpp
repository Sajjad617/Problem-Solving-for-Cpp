
#include <bits/stdc++.h>

using namespace std;

int main() {

    float a, b, c, d, x;
    cin >> a >> b >> c >> d;
    x = (a*b - c*d);
    cout << "DIFERENCA = " << x << endl;
    return 0;
}

