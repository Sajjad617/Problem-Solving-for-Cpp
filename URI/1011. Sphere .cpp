

#include <bits/stdc++.h>

using namespace std;

int main() {

    double pi, r, total;
    pi = 3.14159;
    cin >> r;
    total = (4.0/3)*pi*(r*r*r);
    cout << "VOLUME = " << fixed << setprecision(3) << total << endl;
        return 0;
}

